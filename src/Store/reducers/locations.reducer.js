
const initialState = {
    locations: []
}

const locationsReducer = (state = initialState, action) => {
    switch(action.type) {

        default:
        return state
    }
}

export default locationsReducer